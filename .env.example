# Optional: backend URL when running the app locally against a remote API.
# Leave unset to use Vite dev proxy (see vite.config.ts: server.proxy /api -> localhost:3001).
# VITE_API_URL=http://localhost:3001
